# Core — always needed
sounddevice>=0.4.6        # mic capture + speaker playback (bundles PortAudio)
numpy>=1.24
pyyaml>=6.0
python-dotenv>=1.0
requests>=2.31
onnxruntime>=1.17         # wake word + VAD inference, no torch required
openai>=1.40              # NVIDIA NIM is OpenAI-compatible

# Wake word
openwakeword>=0.6.0

# Speech to text (local). CTranslate2 backend — no torch.
faster-whisper>=1.0.3

# Text to speech (local)
piper-tts>=1.2.0

# Optional: reuse existing MCP servers as tools
# mcp>=1.2.0

# Optional: only if you switch stt.backend to a torch-based model
# torch
# torchaudio
